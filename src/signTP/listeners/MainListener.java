package signTP.listeners;

import org.bukkit.event.Listener;

import signTP.MainClass;

public class MainListener implements Listener {
	MainClass plugin;

	public MainListener(MainClass pl) {
		this.plugin = pl;
	}
}
